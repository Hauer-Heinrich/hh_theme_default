plugin.tx_hhthemedefault {
    view {
        # cat={{EXTENSION_KEY}}/file; type=string; label=Path to template root (FE)
        templateRootPath =

        # cat={{EXTENSION_KEY}}/file; type=string; label=Path to template partials (FE)
        partialRootPath =

        # cat={{EXTENSION_KEY}}/file; type=string; label=Path to template layouts (FE)
        layoutRootPath =
    }
}
