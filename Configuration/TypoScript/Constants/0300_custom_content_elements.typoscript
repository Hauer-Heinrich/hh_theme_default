plugin.tx_hhthemedefault {
    view {
        # cat=hh_theme_default/file; type=string; label=Path to template root (FE)
        templateRootPath =

        # cat=hh_theme_default/file; type=string; label=Path to template partials (FE)
        partialRootPath =

        # cat=hh_theme_default/file; type=string; label=Path to template layouts (FE)
        layoutRootPath =
    }
}
